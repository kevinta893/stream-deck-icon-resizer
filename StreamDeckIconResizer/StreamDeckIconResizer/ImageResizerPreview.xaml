<UserControl x:Class="StreamDeckIconResizer.ImageResizerPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StreamDeckIconResizer"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid>
        <Rectangle x:Name="BackgroundRect" HorizontalAlignment="Stretch" Height="{Binding ActualHeight}" Width="{Binding ActualWidth}" Stretch="None" Fill="#FFDFDFDF" VerticalAlignment="Stretch"/>
        <Image x:Name="BackgroundImage" HorizontalAlignment="Stretch" Height="{Binding ActualHeight}" Stretch="None"/>
        <Image x:Name="ResizeImage" HorizontalAlignment="Stretch" Height="{Binding ActualHeight}" Width="{Binding ActualWidth}" Stretch="None"/>
        <local:ScaleGridOverlay x:Name="ScaleGridOverlay" HorizontalAlignment="Stretch" Height="{Binding ActualHeight}" Width="{Binding ActualWidth}"/>
        <Rectangle x:Name="ForegroundDragDrop" HorizontalAlignment="Stretch" Height="{Binding ActualHeight}" Width="{Binding ActualWidth}" AllowDrop="True" Stretch="Fill" Fill="Transparent"/>
    </Grid>
</UserControl>
