<UserControl x:Class="StreamDeckIconResizer.ImageResizerPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StreamDeckIconResizer"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid>
        <Rectangle x:Name="BackgroundRect" HorizontalAlignment="Left" Height="{Binding ActualHeight, ElementName=Tb}" Width="{Binding ActualWidth, ElementName=Tb}" AllowDrop="True" Stretch="Fill" Fill="#FFDFDFDF"/>
        <Image x:Name="BackgroundImage" HorizontalAlignment="Left" Height="{Binding ActualHeight, ElementName=Tb}" Width="{Binding ActualWidth, ElementName=Tb}" AllowDrop="True" Stretch="Fill"/>
        <Image x:Name="ResizeImage" HorizontalAlignment="Left" Height="{Binding ActualHeight, ElementName=Tb}" Width="{Binding ActualWidth, ElementName=Tb}" AllowDrop="True" Stretch="Fill"/>
    </Grid>
</UserControl>
